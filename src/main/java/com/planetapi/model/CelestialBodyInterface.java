package com.planetapi.model;

public interface CelestialBodyInterface {
    // abstract void checkPhysicalProperties(String properties, String
    // propertyType);

    abstract Double calculateSurfaceArea(Long diameter);
}
